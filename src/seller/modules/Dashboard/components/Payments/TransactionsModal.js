import { Modal } from 'bootstrap'
import React from 'react'

const TransactionsModal = ({isOpen, }) => {
    return (
        <div>
            <Modal
            isOpen={isOpen}
            >

            </Modal>
        </div>
    )
}

export default TransactionsModal
